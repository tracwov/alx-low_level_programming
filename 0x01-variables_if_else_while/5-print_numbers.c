#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print numbers of base 10'
 * Return: always 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 0; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
